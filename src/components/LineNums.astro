---
interface Props {
  count: number;
}

const { count } = Astro.props;
const lines = Array.from({ length: count }, (_, i) => i + 1);
---

<div class="text-ink-faint select-none pr-4 text-right shrink-0 leading-6" style="min-width: 2ch">
  {lines.map((n) => <div>{n}</div>)}
</div>
