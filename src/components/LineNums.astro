---
interface Props {
  count: number;
}

const { count } = Astro.props;
const lines = Array.from({ length: count }, (_, i) => i + 1);
---

<div class="text-slate-600 select-none pr-5 text-right shrink-0" style="min-width: 2ch">
  {lines.map((n) => <div>{n}</div>)}
</div>
